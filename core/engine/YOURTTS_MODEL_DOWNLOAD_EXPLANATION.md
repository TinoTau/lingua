# YourTTS 模型下载说明

## 日志解读

### 1. Conda 警告（可忽略）

```
Unable to create process using 'D:\Program Files\Anaconda\python.exe...
```

**原因**：PowerShell 尝试执行 conda 初始化脚本时失败

**影响**：✅ **无影响**，这只是 PowerShell 的警告，不影响 WSL 中的服务

**解决方案**：可以忽略，或者关闭 PowerShell 的 conda 自动初始化

---

### 2. 服务启动（正常）

```
✅ Using GPU: NVIDIA GeForce RTX 4060 Laptop GPU
📁 Loading YourTTS model from: core/engine/models/tts/your_tts
```

**状态**：✅ **正常**

- GPU 检测成功
- 正在尝试从指定路径加载模型

---

### 3. 模型加载失败，尝试下载（正常流程）

```
⚠️  TTS API loading failed, trying direct load...
> Downloading model to /home/tinot/.local/share/tts/tts_models--multilingual--multi-dataset--your_tts
```

**原因**：
- 本地模型文件不存在或路径不正确
- TTS 库自动从 Hugging Face 下载模型

**这是正常的**：首次使用时会自动下载模型

---

### 4. 模型下载进度

```
10%|███████▌ | 43.4M/425M [00:24<03:00, 2.11MiB/s]
25%|██████████████████▍ | 105M/425M [00:49<02:19, 2.30MiB/s]
```

**含义**：
- 模型总大小：**425 MB**
- 当前进度：25%（105 MB）
- 下载速度：~2.3 MB/s
- 预计剩余时间：约 2 分钟

---

## 这是正常的首次启动流程

### 预期行为

1. **检查本地模型** → 未找到
2. **自动下载模型** → 从 Hugging Face 下载
3. **保存到本地** → `/home/tinot/.local/share/tts/`
4. **加载模型** → 准备就绪

### 下载位置

```
/home/tinot/.local/share/tts/tts_models--multilingual--multi-dataset--your_tts
```

这是 TTS 库的默认缓存目录。

---

## 等待下载完成

### 预期时间

根据当前速度（~2.3 MB/s）：
- 总大小：425 MB
- 预计时间：**约 3-4 分钟**

### 下载完成后

服务会：
1. ✅ 完成模型下载
2. ✅ 加载模型到 GPU
3. ✅ 显示 "✅ YourTTS model loaded successfully"
4. ✅ 服务开始监听请求

---

## 如何加速下载

### 方法 1：使用代理（如果在国内）

```bash
# 设置 Hugging Face 镜像
export HF_ENDPOINT=https://hf-mirror.com
```

### 方法 2：手动下载（如果自动下载太慢）

```bash
# 在 WSL 中
cd /mnt/d/Programs/github/lingua
source venv-wsl-py310/bin/activate

# 使用 huggingface-cli 下载
pip install huggingface_hub
huggingface-cli download coqui/XTTS-v2 --local-dir ~/.local/share/tts/tts_models--multilingual--multi-dataset--your_tts
```

---

## 下次启动

模型下载完成后，下次启动会：
- ✅ 直接从本地加载（非常快）
- ✅ 无需再次下载

---

## 总结

**当前状态**：✅ **正常**

- 服务正在启动
- 首次使用，正在下载模型（约 425 MB）
- 预计 3-4 分钟完成

**需要做的**：
- ⏳ 等待下载完成（这是正常的首次启动流程）
- ✅ 下载完成后服务会自动就绪

**无需担心**：
- Conda 警告可以忽略
- 模型下载是正常流程
- 下次启动会更快

